namespace FashionRise.Domain
{
    /// <summary>Default croquis underlay on the sketch canvas (user can switch or replace with a custom photo).</summary>
    public enum SketchFigureTemplate
    {
        Female = 0,
        Male = 1
    }
}
